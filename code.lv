$const 3

push 0
jmp first
th:
push $const
sub
jmp four
; if it gets parsed correctly then wow

sec:
    push 2
    dup
    .
    jmp th

four:
push 4
jmp first
; some comment

first: push 1
gt
jmpif halt
jmp sec

halt:
dump
halt
